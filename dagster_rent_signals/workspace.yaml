# Dagster workspace configuration for Tampa Rent Signals

load_from:
  - python_package:
      package_name: dagster_rent_signals
      attribute: defs

# Optional: Configure logging
# logging:
#   python_logs:
#     python_log_level: INFO
#     dagster_handler_config:
#       handlers:
#         rotating_file:
#           class: logging.handlers.RotatingFileHandler
#           filename: /tmp/dagster_rent_signals.log
#           maxBytes: 10485760  # 10MB
#           backupCount: 5